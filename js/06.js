//Objecto Math

let resultado;

resultado = Math.PI; // genera el valor de PI 3.14159...

resultado = Math.round(22.4); //redondea el numero 

resultado = Math.ceil(22.4); //redondea el número para arriba 

resultado = Math.floor(22.4); //redondea el numero para abajo 

resultado = Math.sqrt(144); //raíz cuadrada

resultado = Math.abs(-22.4); //vuelve a positivo 

resultado = Math.min(3, 5, 1, 8, 10, 2); //busca el min en un array 

resultado = Math.max(3, 5, 1, 8, 10, 2); //busca el max en un array 

resultado = Math.random(); //genera número random 

resultado = Math.floor(Math.random() * 100);

console.log(resultado);